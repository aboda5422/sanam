# المرحلة الأولى: واجهة العميل - متجر سلة بازار المواسم

## الهوية البصرية
- **الألوان**: أخضر طازج (#1a3c2a، #2d5a3d، #5a8a5c، #a0c49d) مع خلفية بيضاء ناصعة
- **الخطوط**: Outfit للعناوين + DM Sans للنصوص (عصرية، متوافقة مع العربي والموبايل)
- **التخطيط**: hero-grid مثل نينجا - بانر عروض علوي + شبكة أقسام ومنتجات
- **الاتجاه**: RTL كامل، تصميم متجاوب (جوال + كمبيوتر)
- **الحواف**: مستديرة (12px) - عصرية ومرنة

## حفظ ذاكرة المشروع
حفظ كل بيانات الشركة والمتطلبات في ذاكرة المشروع عشان ما ننسى شي في المراحل القادمة

## الصفحات المطلوبة

### 1. الصفحة الرئيسية
- شريط علوي: لوقو + بحث + اختيار العنوان + سلة + تسجيل دخول
- سلايدر عروض (بانرات ترويجية)
- أقسام المنتجات بالصور (خضار، فواكه، ألبان، مخبوزات، مشروبات، حلويات...)
- قسم "الأكثر مبيعاً"

### 2. صفحة القسم
- شبكة بطاقات المنتجات مع فلترة وترتيب
- إضافة سريعة للسلة من البطاقة

### 3. صفحة المنتج
- صورة + وصف + سعر + زر إضافة مع كمية + منتجات مشابهة

### 4. التسجيل وتسجيل الدخول
- تسجيل بالجوال أو الإيميل
- تسجيل دخول + نسيت كلمة المرور
- الملف الشخصي (اسم، عنوان، جوال)

### 5. سلة التسوق
- المنتجات المضافة + تعديل الكمية + الحذف + ملخص الطلب

### 6. إتمام الطلب
- اختيار/إضافة عنوان التوصيل + وقت التوصيل + تأكيد

### 7. متابعة الطلبات
- قائمة الطلبات + حالة كل طلب (قيد التجهيز، في الطريق، تم التسليم)

## التقنيات
- **Lovable Cloud** (Supabase) للباك إند: قاعدة بيانات + مصادقة + تخزين صور
- بيانات تجريبية للمنتجات في البداية
- المراحل القادمة (مناديب، إدارة، تطبيقات موبايل) تُبنى لاحقاً خطوة بخطوة


# المرحلة الثانية: واجهة المندوب (تم ✅)

# المرحلة 3: واجهة إدارة المتجر (Store Admin)

## الخطوة 1: قاعدة البيانات - الجداول الجديدة

### 1.1 الفروع (`branches`)
- `name`, `address`, `lat`, `lng`, `phone`, `is_active`
- **المركز الرئيسي - حي الوسام**: `18.3433, 42.7669`
- **فرع حي شباعة**: `18.3284, 42.7275`

### 1.2 نقل المنتجات والأقسام للداتابيس
- `categories` (name, image, slug, sort_order, is_active)
- `products` (name, description, price, original_price, image, category_id, unit, is_active, is_featured)
- `branch_inventory` (branch_id, product_id, stock_quantity, is_available)

### 1.3 تحديث جدول الطلبات
- إضافة `branch_id`, `payment_method` (cash/card), `collected_amount`

### 1.4 محفظة المندوب
- `driver_wallet` (driver_id, balance, total_collected, total_commission, commission_rate)
- `wallet_transactions` (driver_id, order_id, type, amount, notes)

### 1.5 الشكاوى
- `complaints` (order_id, user_id, type, message, status, admin_reply)

## الخطوة 2: صفحات الإدارة

### 2.1 لوحة التحكم الذكية (`/admin`) - مصفوفة مؤشرات حيوية:
- طلبات اليوم + نسبة التغيير | الإيرادات اليومية | عملاء نشطين | متوسط وقت التوصيل
- طلبات معلقة | مناديب متاحين | شكاوى مفتوحة | مخزون منخفض
- خريطة تتبع مباشر للمناديب + الفروع
- إشعارات تأخر الطلبات

### 2.2 إدارة الطلبات (`/admin/orders`)
### 2.3 إدارة المنتجات (`/admin/products`)
### 2.4 إدارة الأقسام (`/admin/categories`)
### 2.5 إدارة الفروع (`/admin/branches`)
### 2.6 إدارة المناديب (`/admin/drivers`)
### 2.7 إدارة العملاء (`/admin/customers`)
### 2.8 الشكاوى (`/admin/complaints`)
### 2.9 الإعدادات (`/admin/settings`)

## الخطوة 3: التوجيه الذكي (Google Maps)
## الخطوة 4: تحديث واجهة العميل (جلب من الداتابيس)
## الخطوة 5: تحديث واجهة المندوب (المحفظة + التحصيل)

## ترتيب التنفيذ:
1. إنشاء جداول قاعدة البيانات + RLS
2. نقل بيانات المنتجات والأقسام
3. بناء صفحات الإدارة
4. تحديث واجهة العميل
5. تحديث واجهة المندوب
6. منطق التوجيه الذكي
